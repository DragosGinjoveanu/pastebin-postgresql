extends layout

block content
    .card
        .card-body.text-center
            h2 Pastes List
            button.btn.btn-secondary(type='button' onclick="location.href='http://localhost:3000'") Add another paste 
    ul
        each val, index in pastes.authors
            .card
                .card-body
                    h3.card-title(style="color:#585858;")=val + '\'s paste' 
                    h5.card-text= pastes.descriptions[index]
                    -var editUrl = 'http://localhost:3000/pastes/' + pastes.ids[index];
                    -var deleteUrl = 'http://localhost:3000/delete/' + pastes.ids[index];
                    .card-footer
                        form(method="POST" action=deleteUrl)
                            a(class="btn btn-success" href=editUrl) Edit
                            button.btn.btn-danger(type='submit') Delete